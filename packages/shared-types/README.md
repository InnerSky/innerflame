# Shared Types

This package contains TypeScript interfaces and type definitions shared across InnerFlame applications.

## Purpose
- Ensure type consistency across frontend and backend
- Provide a single source of truth for data structures
- Enforce type safety in cross-service communication

## Usage
Types are organized by domain and can be imported directly:

```typescript
import { Entity, EntityType } from '@innerflame/shared-types';
```
